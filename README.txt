1) Run the setup.sh file to have the database, database user, and base table created.

What setup.sh needs
    

Prompt for what you want the database name to be, and load into a variable $database.
Prompt for what	you want the database user to be, and load into	a variable $username.
Prompt for what	you want the database password to be, and load into a variable $password.

mysql $database < 

